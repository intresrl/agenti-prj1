# Shared Types Library

This library contains TypeScript types and interfaces shared between the Angular frontend and NestJS backend, ensuring type safety across the entire stack.

## Usage

Import types from `@food-cost/shared/types`:

```typescript
import { User, Tenant } from '@food-cost/shared/types';
```
